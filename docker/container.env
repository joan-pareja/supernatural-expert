# Appended to the mounted settings file at start, and overriding what it says
# about where PostgreSQL is. A host reaches the database through the port
# published to it; a container on the Compose network reaches the service by name
# on the port it actually listens on. Nothing else differs, so nothing else is
# repeated here.
POSTGRES_HOST=db
POSTGRES_HOST_PORT=5432
